Continue Acorn in `/Users/richard/Developer/github/acorn`.

Read `AGENTS.md` and `session/session-context.md`, then revalidate the checkout,
branch tip, remote, working tree and PR state. The owner has authorized stopping
the agent and viewer, publishing the content-review branch, and merging when CI
and required reviews pass. This supersedes the earlier instruction to keep the
branch unmerged. Complete that delivery if still pending; otherwise continue on
`main` with the owner's next selected topic. Preserve the four separate local
documentation formatting edits described in the context.

The owner prefers a minimal repository containing what is used and useful to
Acorn's mission. The separate AcornSpec evaluator and unused study machinery
have been removed; live constants now belong to Acorn.Constants. Keep the agent
and viewer stopped, retain their checkpoint, and keep Acorn private. Acorn-dev
is immutable. No new research campaign or next implementation task is selected.
